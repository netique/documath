# ~~documath~~ ARCHIVED -- this functionality was implemented in R itself in v4.2.0
Render Math in HTML R Documentation


Injects custom `Rd2HTML` function to enable rendering `\eqn{}` and
    friends with MathJax.

